name: scoring_engine
description: Pure Dart cricket scoring rules engine with event-sourced match state.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  collection: ^1.18.0
  meta: ^1.15.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
